# Quantlerning — 从 0 到量化高手的可视化学习网站

## 0. 真实性原则（最高优先级）

> **一切数据与内容必须真实。** 这是项目的核心底线。

1. **数据可视化**必须基于真实数据源（QuantLab 库的行情/财务/宏观/因子），禁止为效果编造数字
2. **教学示意**（如 DDM 例子中的"股息 2 元"）允许存在，但必须明确标注"示意/简化举例"，不得冒充真实数据
3. 页面占位数据（如示例因子）**禁止**——必须替换为真实查询，或明确标注"待接入真实数据"
4. 公式、理论内容必须准确，不得为通俗而扭曲
5. 真实数据不可用时，宁可显示"数据不可用"，也不要造假

## 1. 项目定位

一个**渐进式、可视化优先**的量化学习网站。用户（本人）按天/按阶段学习金融与量化知识，每个核心概念配交互可视化、Python 练习与测验，学完能独立完成「数据 → 因子 → 策略 → 组合 → 风控 → 实盘」全 pipeline。

**差异化核心**：所有抽象概念都「能看见、能动手」——拖动滑块看夏普比率变化、拖动权重看有效前沿移动、点击运行看蒙特卡洛路径生成。

**受众**：本人（先自用），后续可扩展为对外学习产品。

## 2. 技术架构

### 2.1 总体形态

```
┌─────────────────────────────────────────────────────────────┐
│  Frontend: Vue 3 + Vite + TypeScript                        │
│  ┌──────────────┐ ┌───────────────┐ ┌──────────────────┐    │
│  │ 课程页面      │ │ 可视化组件库    │ │ 代码练习区        │    │
│  │ 进度/讲解/测验│ │ ECharts+Canvas │ │ 提交→后端沙箱执行  │    │
│  └──────────────┘ └───────────────┘ └──────────────────┘    │
└─────────────────────────┬────────────────────────────────────┘
                          │ REST API (http://localhost:8100)
┌─────────────────────────▼────────────────────────────────────┐
│  Backend: FastAPI (独立 venv，复用 QuantLab 依赖)             │
│  ├─ content 服务   课程内容(API生成+审核)、进度、测验          │
│  ├─ data 服务      只读查询 quantlab 库（股票/财务/宏观/ETF）  │
│  ├─ compute 服务   回测/定价/因子/组合 计算 → 喂给可视化        │
│  ├─ sandbox 服务   受限执行用户 Python 练习代码                 │
│  └─ auth 服务      简单本地登录（单用户起步）                   │
└─────────────────────────┬────────────────────────────────────┘
                          │ SQLAlchemy（read-only 连接）
              ┌───────────▼───────────┐
              │ PostgreSQL quantlab 库 │  10年A股日线/财务/宏观/ETF/因子
              └───────────────────────┘
```

### 2.2 关键决策

| 决策点 | 结论 | 理由 |
|---|---|---|
| 项目形态 | 独立新项目于 `Quantlerning/` | 职责清晰，不污染 QuantLab 研究平台 |
| 数据库 | 只读 QuantLab 的 `quantlab` 库 | 真实 10 年数据，教学可信度高 |
| 后端环境 | 独立 venv（`Quantlerning/.venv`） | 隔离沙箱代码与 QuantLab 依赖，版本风险低 |
| 前端技术 | Vue 3 + ECharts + Canvas 模拟器 | 与 QuantLab 一致，组件经验可迁移 |
| 内容生产 | AI 生成 + 本人审核 | 内容量大，纯手写不可行 |
| 练习运行 | 后端沙箱执行 | 体验好，学习即动手 |
| 数据源 | 优先真实 DB 数据，模拟数据兜底 | 真实数据可信，模拟数据可控（如 BS 定价） |

### 2.3 学习站自己的数据

学习站**只读** QuantLab 库，但需要少量自己的表（放 `quantlerning` 库）：

| 表 | 用途 |
|---|---|
| `lesson` | 课程元数据（id、阶段、标题、顺序、状态） |
| `quiz_result` | 测验答题记录 |
| `exercise_result` | 练习提交记录 |
| `progress` | 学习进度（每课完成时间、可视化操作记录） |
| `content_review` | AI 生成内容的审核状态（待审/通过/修改） |

## 3. 目录结构

```
Quantlerning/
├── PLAN.md                     # 本规划
├── backend/
│   ├── app/
│   │   ├── main.py             # FastAPI 入口 (port 8100)
│   │   ├── core/
│   │   │   ├── config.py       # 配置（读取 ../.env）
│   │   │   ├── db.py           # 只读连接 quantlab 库
│   │   │   └── security.py     # 本地鉴权（可选）
│   │   ├── api/
│   │   │   ├── courses.py      # 课程内容/进度
│   │   │   ├── data.py         # 行情/财务/宏观查询
│   │   │   ├── compute.py      # 回测/定价/因子计算
│   │   │   ├── exec.py         # 练习代码执行
│   │   │   └── quiz.py         # 测验
│   │   ├── services/
│   │   │   ├── content/        # 课程生成（AI）、内容审核
│   │   │   ├── data/           # 行情/财务/宏观/因子 查询封装
│   │   │   ├── compute/        # 回测、定价、因子、组合计算
│   │   │   └── sandbox/        # 练习执行沙箱
│   │   └── content/            # 课程定义（Markdown + YAML frontmatter）
│   │       ├── phase1/         # 金融市场基础
│   │       ├── phase2/         # 回测
│   │       ├── phase3/         # 因子
│   │       ├── phase4/         # 衍生品
│   │       ├── phase5/         # ML
│   │       └── phase6/         # 组合优化
│   ├── .env                    # 数据库连接配置（复用 QuantLab .env 或独立）
│   └── requirements.txt
├── frontend/
│   ├── src/
│   │   ├── main.ts
│   │   ├── router/index.ts
│   │   ├── stores/progress.ts  # 进度状态（localStorage 持久化）
│   │   ├── views/
│   │   │   ├── HomeView.vue        # 学习地图/总览
│   │   │   ├── PhaseView.vue       # 阶段列表
│   │   │   ├── LessonView.vue      # 课程详情（讲解+可视化+练习+测验）
│   │   │   └── LabView.vue         # 可视化实验室（自由探索）
│   │   ├── components/
│   │   │   ├── charts/             # ECharts 封装（K线/净值/直方图/热力图/3D）
│   │   │   ├── simulators/         # Canvas 交互模拟器
│   │   │   ├── lesson/             # 讲解/练习/测验 组件
│   │   │   └── common/
│   │   └── api/                    # axios 封装
│   ├── package.json
│   └── vite.config.ts
└── README.md
```

## 4. 课程体系（Phase 0-6）

### 4.1 Phase 0 数学与编程基础（✅ 已完成，跳过）

概率论、数理统计、线性代数、微积分、计量经济学、Python。作为前置知识，不进入课程站；但学习站内提供「速查表」页面（公式/API 备忘）。

### 4.2 Phase 1 金融市场基础 + 金融学 + 数据获取（3~4 周）

**第一部分：金融学基础（新增，打理论骨架）**

| 课程 | 核心知识点 | 可视化 | 练习 |
|---|---|---|---|
| 1.1 货币时间价值 | 现值/终值/复利/年金 | 折现曲线可视化（拖动 r/n 看现值变化） | 计算 PV/FV |
| 1.2 利率与折现率 | 名义/实际利率、风险溢价 | 费雪方程可视化 | 计算实际利率 |
| 1.3 债券与久期 | YTM/久期/收益率曲线 | 债券价格-利率曲线 + 久期敏感度滑块 | 计算久期 |
| 1.4 股票估值模型 | DCF/DDM/相对估值 | DDM 估值滑块（D/r/g） | 戈登模型计算 |
| 1.5 CAPM | β/市场风险溢价/SML | 🔥 β 滑块 → 收益-风险散点 + SML 线 | 回归算 β |
| 1.6 有效市场假说 | EMH 三形态/异象 | 三种市场形态示意对比 | 案例分析 |

**第二部分：市场与指标**

| 课程 | 核心知识点 | 可视化 | 练习 |
|---|---|---|---|
| 1.7 投资流派总览 | 价值/成长/趋势/套利/量化流派 | 流派关系脑图 | 无 |
| 1.8 收益率 | 单期/多期/对数收益、复利 | 真实K线→收益曲线；对数vs算术收益对比 | 计算收益序列 |
| 1.9 波动率 | 历史波动、年化、波动聚集 | 滚动波动率曲线 + 收益分布直方图 | 计算年化波动 |
| 1.10 夏普比率 | 定义、含义、局限 | 🔥 Sharpe 滑块模拟器（σ/μ/rf 拖动） | 计算并比较 |
| 1.11 最大回撤 | 定义、回撤期间 | 净值曲线回撤阴影区 + 水下区标注 | 计算 MDD |
| 1.12 宏观/利率 | 利率/CPI/PPI/GDP | 宏观指标趋势图（DB 真实数据） | 读取宏观数据 |
| 1.13 技术面 MACD | DIF/DEA/柱状 | 真实K线 + 慢/快线参数滑块 | 手算 MACD |
| 1.14 技术面 RSI | 超买超卖 | K线 + RSI 带（阈值可调） | 计算 RSI |
| 1.15 基本面 PE/PB | 估值、PIT、杜邦 | 全市场 PE/PB 分布散点 + 个股定位 | 查估值分位 |

**第三部分：数据获取与工具**

| 课程 | 核心知识点 | 可视化 | 练习 |
|---|---|---|---|
| 1.16 数据获取 | Baostock/Tushare | 数据同步流程示意 | 拉取数据 |
| 1.17 Pandas 处理 | 清洗/重采样/合并 | 数据表格交互展示 | 练习题 |
| 1.18 阶段产出 | 组合所学 | 完整个股分析仪表盘 | 分析报告 |

**产出**：理解金融学核心理论（估值/定价/市场有效性），能独立拉取 A 股数据，理解 MACD/RSI/PE/PB 等指标含义。

### 4.3 Phase 2 量化策略入门 + 回测（3~4 周）

| 课程 | 核心知识点 | 可视化 | 练习 |
|---|---|---|---|
| 2.1 策略是什么 | 信号/持仓/交易 | 策略流水线示意 | 无 |
| 2.2 趋势跟踪 | 均线交叉、唐奇安 | K线 + 均线交叉买卖点标注 | 实现均线策略 |
| 2.3 均值回归 | 布林带、z-score | 布林带动画 + 回归信号 | 实现布林策略 |
| 2.4 动量策略 | 横截面动量、时序动量 | 动量排序热力图 | 实现动量排名 |
| 2.5 回测引擎 | Backtrader 入门 | 引擎运行流程 | 跑通首个回测 |
| 2.6 绩效指标 | 夏普/回撤/胜率/换手 | 绩效报告面板 | 解读报告 |
| 2.7 回测陷阱 | 前视偏差/幸存者偏差 | 偏差演示对比 | 识别偏差 |
| 2.8 交易成本 | 佣金/滑点/冲击 | 成本对净值影响对比 | 成本敏感分析 |
| 2.9 三策略对比 | 横向比较 | 三策略叠加净值 + 指标表 | 对比分析 |
| 2.10 阶段产出 | 独立回测 | 策略报告仪表盘 | 完整回测报告 |

**产出**：用 Backtrader 跑通 3 个以上策略回测，输出绩效报告。

### 4.4 Phase 3 因子投资与选股模型（4~5 周）

| 课程 | 核心知识点 | 可视化 | 练习 |
|---|---|---|---|
| 3.1 因子是什么 | 因子 vs 策略 | 因子逻辑示意 | 无 |
| 3.2 APT 模型 | 多因子收益来源 | 因子暴露图 | 实现因子回归 |
| 3.3 三因子/五因子 | Fama-French | 因子市值/账面比分组 | 计算因子收益 |
| 3.4 单因子检验 | IC/IR/分层收益 | 🔥 真实因子（DB 161个）IC 序列 + 分层收益热力图 | 因子评估 |
| 3.5 因子中性化 | 市值/行业中性 | 中性前后对比 | 中性化处理 |
| 3.6 多因子选股 | 打分/加权合成 | 打分流程可视化 | 合成综合因子 |
| 3.7 因子拥挤度 | 换手/相关性 | 因子相关性热力图 | 相关性分析 |
| 3.8 Barra 风险模型 | 风险因子暴露 | 风险因子暴露雷达图 | 风险归因 |
| 3.9 因子挖掘 | 符号回归/自动化 | 候选因子评估流程 | 挖掘实验 |
| 3.10 阶段产出 | 搭建多因子框架 | 多因子回测仪表盘 | 回测年化超额 |

**产出**：搭建多因子选股框架，回测年化超额收益。（可直接复用 QuantLab 现有因子库）

### 4.5 Phase 4 衍生品定价与风险管理（4~6 周）

| 课程 | 核心知识点 | 可视化 | 练习 |
|---|---|---|---|
| 4.1 随机过程 | 布朗运动/几何布朗运动 | 随机游走动画 | 模拟路径 |
| 4.2 伊藤引理 | 引理推导 | 引理结构示意 | 推导练习 |
| 4.3 Black-Scholes | 公式、假设 | 🔥 BS 定价滑块（S/K/σ/T/r）+ 3D 曲面 | 实现 BS 定价 |
| 4.4 二项式定价 | 二叉树 | 二叉树动画（步数滑块） | 实现二叉树 |
| 4.5 蒙特卡洛定价 | 路径模拟 | 🔥 路径动画 + 收敛分布 | 实现 MC 定价 |
| 4.6 Greeks | Delta/Gamma/Vega | Greeks 敏感度曲线滑块 | 计算 Greeks |
| 4.7 波动率微笑 | 隐含波动率 | 波动率曲面 3D | 拟合微笑 |
| 4.8 VaR | 参数法/历史法/MC | 收益分布 + 尾部着色 + CI 滑块 | 计算 VaR |
| 4.9 对冲 | Delta 中性 | 对冲前后组合损益 | 对冲模拟 |
| 4.10 阶段产出 | 期权定价器 | 定价器交互面板 | 完整定价器 |

**产出**：用 Python 实现期权定价器，计算 Greeks 和 VaR。

### 4.6 Phase 5 机器学习量化（6~8 周）

| 课程 | 核心知识点 | 可视化 | 练习 |
|---|---|---|---|
| 5.1 ML 概览 | 监督/无监督/强化 | 方法图谱 | 无 |
| 5.2 特征工程 | 因子构造/标准化 | 特征分布 + 相关性热力图 | 构造特征 |
| 5.3 标签设计 | 前瞻收益/事件 | 标签分布 | 设计标签 |
| 5.4 模型选择 | XGBoost/LightGBM | 特征重要性 + 树结构 | 训练模型 |
| 5.5 过拟合防范 | 交叉验证/正则 | 学习曲线 + 训练/验证损失 | 防止过拟合 |
| 5.6 时间序列模型 | LSTM | 预测 vs 真实走势 | 训练 LSTM |
| 5.7 模型评估 | IC/分类指标 | 预测 vs 实际散点 + IC 图 | 评估模型 |
| 5.8 Alpha 挖掘 | 挖掘流程 | 接 QuantLab 挖掘结果可视化 | 挖掘实验 |
| 5.9 强化学习 | RL 框架 | 环境-智能体交互示意 | RL 训练 |
| 5.10 阶段产出 | ML 策略对比 | ML vs 传统策略对比 | 完整策略 |

**产出**：用 ML 模型预测涨跌/构建 Alpha 因子，对比传统策略。

### 4.7 Phase 6 组合优化 + 实盘（持续）

| 课程 | 核心知识点 | 可视化 | 练习 |
|---|---|---|---|
| 6.1 马科维茨 | 有效前沿/最小方差 | 🔥 权重滑块 → 实时有效前沿 | 求解前沿 |
| 6.2 风险平价 | 波动率平价 | 配置对比图 | 实现风险平价 |
| 6.3 Black-Litterman | 观点融合 | 观点 → 后验权重变化 | 实现 BL |
| 6.4 统计套利 | 配对交易/协整 | 协整散点 + 价差 z-score | 配对检验 |
| 6.5 组合风控 | 止损/仓位/压力测试 | 压力情景组合损益 | 风控规则 |
| 6.6 模拟盘 | 模拟交易 | 模拟盘净值监控 | 模拟交易 |
| 6.7 实盘准备 | 券商 API/风控 | 上线检查清单 | 部署脚本 |
| 6.8 市场微观结构 | 订单簿/流动性 | 订单簿深度图 | 分析订单簿 |
| 6.9 高频基础 | 延迟/撮合 | 撮合流程示意 | 模拟撮合 |
| 6.10 阶段产出 | 完整 pipeline | 全流程仪表盘 | 实盘策略 |

**产出**：跑通完整量化 pipeline：数据→因子→策略→组合→风控→实盘。

## 5. 可视化组件库设计

### 5.1 ECharts 封装组件（`frontend/src/components/charts/`）

| 组件 | 用途 | 数据接口 |
|---|---|---|
| `KlineChart.vue` | 真实K线（含均线/指标叠加） | `[{date,open,high,low,close,volume}]` |
| `LineChart.vue` | 净值/指数曲线（多序列叠加） | `{series:[{name,data[]}], x[]}` |
| `Histogram.vue` | 收益分布/IC 分布 | `{bins[], counts[]}` |
| `Heatmap.vue` | 分层收益/相关性矩阵 | `{rows[], cols[], values[][]}` |
| `ScatterChart.vue` | PE/PB 散点、预测vs实际 | `{x[], y[], size?, color?}` |
| `Surface3D.vue` | BS 价格曲面/波动率曲面 | `{x[], y[], z[][]}` |
| `RadarChart.vue` | Barra 风险暴露 | `{dimensions[], values[]}` |
| `Timeline.vue` | 事件时间线 | `{events[{date,label}]}` |

### 5.2 Canvas 交互模拟器（`frontend/src/components/simulators/`）

| 模拟器 | 交互 | 教学点 |
|---|---|---|
| `SharpeSimulator.vue` | 拖 μ/σ/rf 滑块 | 夏普=风险调整后收益，直观理解「性价比」 |
| `DrawdownSimulator.vue` | 拖动查看回撤区间 | 水下区=回撤深度/时长 |
| `MACDSimulator.vue` | 拖快/慢/信号参数 + 选股 | 参数敏感性 |
| `BollingerSimulator.vue` | 拖周期/倍数 | 布林带收口=低波动，扩张=高波动 |
| `MCSimulator.vue` | 路径数/步数滑块 + 动画 | 大数定律，MC 收敛 |
| `BSTreeSimulator.vue` | 步数滑块动画 | 二叉树收敛到 BS |
| `BSPricingSimulator.vue` | S/K/σ/T/r 滑块 + 3D | BS 公式直觉 |
| `FrontierSimulator.vue` | 权重滑块 → 实时前沿 | 分散化「免费午餐」 |
| `PairTradingSimulator.vue` | 选配对 + 阈值滑块 | 价差均值回归 |
| `RandomWalkSimulator.vue` | 步数/股数滑块动画 | 布朗运动 |

### 5.3 可视化数据接口约定

每个模拟器/图表对应后端一个 compute API，返回 JSON 数据。约定：

```json
// GET /api/v1/compute/sharpe?mu=0.15&sigma=0.25&rf=0.02
{
  "sharpe": 0.52,
  "excess_return": 0.13,
  "risk": 0.25,
  "curve_points": [...],  // 供图表绘制
  "status": "ok"
}
```

**性能约定**：真实 DB 查询加缓存（TTL 5 分钟）；大结果集（10 年日线）服务端预聚合后再传。

## 6. 课程内容定义格式

每课一个 Markdown 文件 + YAML frontmatter：

```yaml
---
id: p1-l4
phase: 1
order: 4
title: 夏普比率
duration_hours: 1.5
concepts: [sharpe_ratio, excess_return, volatility]
visualization: sharpe_simulator   # 对应 Canvas 组件名
compute_api: /api/v1/compute/sharpe
exercises:
  - id: p1l4e1
    prompt: "计算给定 μ=0.12, σ=0.20, rf=0.03 的夏普比率"
    solution_check: "abs(round(user_ans,2) - 0.45) < 0.01"
quiz:
  - q: "夏普比率衡量的是什么？"
    options: [A, B, C, D]
    answer: C
    explanation: "..."
ai_generated: true
review_status: pending   # pending/approved/edited
---
## 概念讲解
...

## 动手试试
（可视化说明）

## 代码练习
（Python 示例）
```

内容字段 AI 生成，`review_status` 标记待审核。

## 7. API 设计（v1）

### 7.1 课程

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/v1/courses` | 课程地图（阶段/课程/进度） |
| GET | `/api/v1/courses/{id}` | 课程详情（含内容/练习/测验） |
| GET | `/api/v1/courses/{id}/content` | 课程讲解内容 |
| POST | `/api/v1/courses/{id}/progress` | 更新学习进度 |
| POST | `/api/v1/courses/{id}/quiz` | 提交测验 |
| GET | `/api/v1/content/pending` | 待审核内容列表 |
| PUT | `/api/v1/content/{id}/review` | 审核内容 |

### 7.2 数据（只读 quantlab 库）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/v1/data/stock/{code}/daily` | 个股日线（参数：start/end/adjust） |
| GET | `/api/v1/data/stock/{code}/indicator` | 个股财务指标 |
| GET | `/api/v1/data/macro/{indicator}` | 宏观指标 |
| GET | `/api/v1/data/index/{code}` | 指数日线 |
| GET | `/api/v1/data/etf/{code}/daily` | ETF 日线 |
| GET | `/api/v1/data/universe` | 股票池/行业列表 |
| GET | `/api/v1/data/stock/{code}/valuation` | 估值分布（PE/PB 分位） |
| GET | `/api/v1/data/search?q=` | 股票搜索 |

### 7.3 计算

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/v1/compute/sharpe` | 夏普计算 |
| GET | `/api/v1/compute/mdd` | 回撤计算 |
| GET | `/api/v1/compute/macd` | MACD 指标 |
| GET | `/api/v1/compute/rsi` | RSI 指标 |
| GET | `/api/v1/compute/backtest` | 简易回测（参数化） |
| GET | `/api/v1/compute/factor-eval/{id}` | 因子评估（复用 QuantLab 因子） |
| POST | `/api/v1/compute/black-scholes` | BS 定价 |
| POST | `/api/v1/compute/monte-carlo` | MC 定价路径 |
| POST | `/api/v1/compute/frontier` | 有效前沿 |
| POST | `/api/v1/compute/var` | VaR 计算 |
| POST | `/api/v1/compute/cointegration` | 协整检验 |

### 7.4 代码执行

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/v1/exec/run` | 执行练习代码，返回 stdout/结果/错误 |

**沙箱约束**：
- 超时 10s，内存上限 256MB，CPU 限制 1 核
- 禁用网络（socket 关闭）、文件系统写（临时目录除外）
- 禁用 os/subprocess/sys/module 等危险模块
- 仅预载数据访问对象（如 `get_daily('000001')` 走只读 API）
- 白名单库：pandas, numpy, math, statistics

## 8. 开发里程碑

| 里程碑 | 内容 | 验收标准 |
|---|---|---|
| **M0** | 项目骨架 | 前后端启动，DB 只读连接通，首页课程地图可渲染 |
| **M1** | Phase 1 全 12 课 + 4 个模拟器 | Sharpe/回撤/MACD/RSI 模拟器可交互，练习沙箱可用 |
| **M2** | Phase 2 全 10 课 | 三策略回测可视化 + 绩效报告面板 |
| **M3** | Phase 3 全 10 课 | 真实因子评估可视化（用 DB 161 因子） |
| **M4** | Phase 4 全 10 课 | BS/MC/二叉树模拟器 + VaR |
| **M5** | Phase 5 全 10 课 | ML 训练可视化 + 沙箱 ML 练习 |
| **M6** | Phase 6 全 10 课 | 有效前沿模拟器 + 全流程仪表盘 |

每个里程碑结束：内容审核通过、可视化可交互、练习可运行、测验可提交。

## 9. 风险与对策

| 风险 | 对策 |
|---|---|
| 内容量巨大（55 课） | AI 批量生成 + 分阶段推进，每阶段只做当阶段内容 |
| AI 内容质量 | 全部标记 `review_status`，本人审核后才发布 |
| 沙箱安全 | 严格白名单 + 资源限制 + 无网络 + 只读数据 API |
| 真实数据查询慢 | 服务端聚合 + 缓存 + 只查需要的字段 |
| 学习站改动影响 QuantLab | 只读连接（只读事务），独立部署端口，独立 venv |
| 可视化开发量大 | 先做通用 ECharts 封装，模拟器按优先级逐个做 |

## 10. 启动步骤

1. 初始化 `Quantlerning/` 仓库结构（backend + frontend）
2. 新建独立 venv（`Quantlerning/.venv`）
3. 后端：FastAPI + 只读 DB 连接 + 课程元数据 API
4. 前端：Vue 3 脚手架 + 首页课程地图 + 阶段/课程路由
5. 先做 Phase 1 前 5 课内容 + Sharpe/回撤模拟器
6. 验证全链路后，按里程碑推进
