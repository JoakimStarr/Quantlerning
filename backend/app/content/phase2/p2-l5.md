---
id: p2-l5
title: Web 化回测引擎
summary: "策略规则写好了，怎么知道它赚不赚钱？——在历史数据上「重放」一遍。回测就是这件事：如果这套规则从 2020 年跑到今天，结果会怎样。"
---

## 回测的定义

先给定义：回测（Backtesting）是在历史数据上模拟一段策略的完整生命——从信号生成、仓位映射、成交得价，到逐日净值累计。它回答一个问题——**「如果这套规则从过去跑到现在，结果如何？」**

**回测不能预测未来**。未来可能出现历史里没有出现过的情况（新的制度、新的市场结构），回测只能「排除明显站不住脚的想法」，不能保证未来盈利。它的价值在于：让后面的「看」和「疑」有对象——先跑出结果，再判断结果靠不靠得住。

回测的四个组成部分：

1. **数据**：真实历史行情（本课程用 quantlab 库，复权口径）；
2. **策略规则**：p2-l1 的信号 → 持仓 → 交易三段；
3. **撮合**：信号出现后按什么价格成交（本课程统一「次日开盘价」，避免前视）；
4. **绩效**：净值曲线与各项指标（p2-l6）。

## 朴素回测引擎

一个最小的回测引擎只需四个循环步骤，用真实茅台数据跑均线交叉：

```
初始化：cash = 首日资金，position = 0（空仓）
逐日循环：对每一天 t
    1. 由 t-1 收盘的均线关系计算今日信号
    2. 若持币且有买入信号 → 按今日开盘价买入
    3. 若持仓且有卖出信号 → 按今日开盘价卖出
    4. 用今日收盘价标记市值，记录净值
```

:::viz backtest_engine code=sh600519 start=2020-01-01 end=2026-08-10 caption=朴素回测逐日重放（真实茅台 2020-2026）：上图为金叉/死叉信号驱动下的持仓状态与成交点；下图净值曲线由「当日持仓 × 当日涨跌」逐日累计。每一步都可查、可复核。
:::

## 本课程回测口径

为了让所有策略横向可比，本课程统一三个口径：

| 项目 | 约定 |
|---|---|
| 数据 | quantlab 真实日线，复权 pct_chg |
| 成交价 | 信号**次日开盘价**（当日看到信号当日成交即前视） |
| 收益口径 | 复利连乘、不含首日、ddof=1、几何年化 |

任何策略在本课程中的回测，都在这三个约定下进行。这也是第二章所有对比表格的数字来源。

> 注：策略信号（均线/动量/布林带）用**未复权收盘价**计算，与收益用的复权 pct_chg 口径不同——两者在分红除权日会有微小差异（除权日未复权价「跳跌」，可能诱发假信号）。这是本课程的教学简化口径，第一章 p1-l10 对技术指标有同样的说明。

## 回测引擎的进阶

真实引擎还需处理更多：涨跌停不可成交、停牌、最小交易单位、融资融券、滑点与佣金（p2-l8）。业界常用 Backtrader / vn.py / zipline 等框架，它们把「撮合」「成本」「绩效」抽象成可配置模块——但**框架只是工具，口径才是灵魂**。

一个回测结果是否可信，取决于你能回答三个问题：数据是否真实？成交是否可实际发生？指标口径是否统一？下一课讲绩效指标，再下一课讲回测最容易踩的坑。

## 回测的统计视角

回测得到的年化收益、夏普不是「确定值」，而是**样本估计**——它来自有限的一段历史，本身有误差。把策略日收益当成随机样本，统计量就有标准误：

$$SE(\text{年化收益})\approx\frac{\sigma_{ann}}{\sqrt{n/252}},\qquad SE(Sharpe)\approx\sqrt{\frac{1+Sharpe^2/2}{n}}$$

其中 $n$ 是回测天数。含义：

- 茅台 2020-2026 约 1600 个交易日，若策略年化波动 18%，年化收益的约 1 个标准误约 $18\%/\sqrt{1600/252}\approx7.1\%$——**±7 个百分点的波动，完全可能把 4.4% 的均线年化「变」成 11% 或 -3%**；
- 夏普的近似标准误 $SE(Sharpe)\approx\sqrt{(1+Sharpe^2/2)/n}$：夏普 0.2、1600 天时约 $\sqrt{1.02/1600}\approx0.025$，95% 置信区间约 ±0.05——夏普 0.20 与 0.15 在这段样本上几乎没有区分度。

**回测的统计结论**：样本越短，指标越不可信；「5.6% 年化」与「7.4% 年化」的差异，在 1600 天样本里可能只是噪声。所以：
- 永远报回测天数与日数（样本量）；
- 对比策略时看置信区间而非单个数字；
- 长样本（跨牛熊）比短样本可信，这也是第二章把窗口拉到 2020-2026 的原因。

## 训练集与测试集

回测的统计视角引出一个关键问题：**既然样本估计有噪声，如何知道策略是真的有效，还是只是在这段历史上碰巧有效？** 答案是把数据切成两段：

- **训练集（in-sample）**：用来「找」策略与调参数——比如扫描均线周期、确定 lookback 窗口；
- **测试集（out-of-sample）**：策略定稿之后，用这段「没见过」的数据做最终检验。

如果策略只在训练集上赚钱、一到测试集就失效，说明它学到的不是规律，而是训练集的噪声（过拟合，p2-l7 专讲）。**先猜后验、数据说了算**：训练集负责「猜」，测试集负责「验」——验证必须用没参与过调参的数据，否则就是「考自己出过的题」。

本课程的策略都是规则型的（均线、唐奇安、动量），没有显式的「训练」步骤，但同样的原则适用：不要在整段历史上反复调参直到好看为止，要留出样本外窗口检验。

## 练习题

**练习 1（选择题）：** 回测回答的核心问题。

:::quiz
Q: 回测回答的核心问题是?
- 这套规则从过去跑到现在，结果如何
- 未来一个月股票怎么走
- 哪种交易软件最好用
- 参数如何取值最优
A: 1
E: 回测在历史数据上模拟信号→持仓→交易→净值的完整生命，回答「如果这套规则从过去跑到现在，结果如何」。
:::

**练习 2（选择题）：** 回测的四个组成部分。

:::quiz
Q: 回测的四个组成部分是?
- 数据、策略规则、撮合、绩效
- 数据、资金、风控、净值
- 信号、持仓、交易、成本
- 均线、动量、回归、趋势
A: 1
E: 回测四要素：真实数据、策略规则（信号→持仓→交易）、撮合（成交价约定）与绩效（净值与指标）。
:::

**练习 3（选择题）：** 朴素回测引擎的第一步。

:::quiz
Q: 朴素回测引擎逐日循环的第一步是?
- 用今日开盘价直接成交
- 由 t-1 收盘的均线关系计算今日信号
- 记录今日净值
- 计算最大回撤
A: 2
E: 信号由 t-1 收盘计算、t 日执行，避免把当日信息提前用于当日决策，这是引擎防止前视的关键。
:::

**练习 4（选择题）：** 净值记录一步的作用。

:::quiz
Q: 朴素引擎中「用今日收盘价标记市值、记录净值」这一步的作用是?
- 决定次日的交易信号
- 把持仓按当日收盘价换算成账户市值，逐日累积出净值曲线
- 提交成交委托
- 计算交易成本
A: 2
E: 净值 = 现金 + 持仓 × 当日收盘价；逐日记录才能画出净值曲线并计算各项绩效指标。
:::

**练习 5（应用题）：** 手动计算逐日净值。

:::exercise
T: 设某策略逐日生效的持仓序列为 $pos=[0,1,1,0]$（第 1 日空仓），四日收盘收益为 $r=[+1\%,-2\%,+3\%,+0.5\%]$。请按「策略日收益 $=$ 当日收益 $\times$ 当日持仓」计算每天的策略收益，并按 $nav=(1+strat).cumprod()$ 累乘出前 4 日的净值；并说明第 1 日为什么策略收益为 0。

（追问）若持仓序列改为 $pos=[1,0,1,0]$（第 1 日满仓），第 2 日与第 4 日的净值各是多少？第 4 日市场 +0.5% 为什么完全没被策略捕获？这说明了持仓映射对净值的什么影响？

:::

**练习 6（选择题）：** 本课程的成交价约定。

:::quiz
Q: 本课程统一采用的成交价约定是?
- 信号当日收盘价
- 信号次日开盘价
- 信号当日开盘价
- 信号次日收盘价
A: 2
E: 信号次日开盘价成交：当日看到信号、当日成交即前视。统一口径才能让所有策略横向可比。
:::

**练习 7（选择题）：** 回测框架与口径的关系。

:::quiz
Q: 关于回测框架与口径的关系，下列说法正确的是?
- 框架比口径更重要
- 框架只是工具，口径才是灵魂——可信与否取决于数据真实、成交可实现、指标口径统一
- 只要用了 Backtrader 结果就必然可信
- 口径是否统一不影响结果可比性
A: 2
E: 业界框架只提供可配置的撮合/成本/绩效模块，回测可信度取决于能否回答数据、成交、指标三个问题。
:::

**练习 8（应用题）：** 口径统一对横向对比的意义。

:::exercise
T: 两支策略在同一标的、同一窗口回测：A 用「信号当日收盘价成交」，B 用「信号次日开盘价成交」，B 的累计收益显著低于 A。请判断这种差异主要来自策略本身还是口径？若要把两条策略放同一张表对比，应该如何处理？结合本课三个统一口径（数据、成交价、收益口径）说明。

（追问）若把 B 也改成「信号当日收盘价成交」后，B 的收益仍低于 A，此时差异才能归因于什么？「统一口径」是横向比较的充分条件还是必要条件？为什么？

:::

**练习 9（选择题）：** 回测指标是样本估计。

:::quiz
Q: 为什么「5.6% 与 7.4% 的年化差异」在 1600 天回测里可能只是噪声?
- 因为年化收益的计算公式有问题
- 因为年化收益是样本估计，约 1 个标准误可能达 ±7 个百分点，差异未超出噪声范围
- 因为 1600 天太短无法计算
- 因为策略日收益不服从正态
A: 2
E: $SE(\text{年化})\approx\sigma_{ann}/\sqrt{n/252}$，波动 18% 时约 ±7 个百分点；4.4% 与 7.4% 的差距在一个标准误内，统计上不显著。
:::

**练习 10（选择题）：** 回测样本量与结论可信度。

:::quiz
Q: 同一条策略在 500 天样本上年化收益 8%，在 1600 天样本上年化收益 5%。下列说法最合理的是?
- 策略在短样本上更有效
- 短样本的标准误更大，$SE\approx\sigma_{ann}/\sqrt{n/252}$，8% 与 5% 的差异可能只是噪声；长样本估计更可信
- 长样本说明策略已经失效
- 取两次平均值 6.5% 就是真值
A: 2
E: 年化收益是样本估计，样本越短标准误越大；差异可能在 1 个标准误内，这正是第二章把窗口拉到 2020-2026 的原因。
:::

**练习 10（选择题）：** 训练集与测试集。

:::quiz
Q: 关于训练集（in-sample）与测试集（out-of-sample），下列说法正确的是?
- 用整段历史调参后，再在整段历史上评价策略，是有效的验证
- 测试集用于「找」策略与调参数，训练集用于最终检验
- 训练集用来调参数，测试集用「没见过」的数据做最终检验，防止把噪声当规律
- 训练集和测试集可以重叠使用
A: 3
E: 训练集负责「猜」（调参），测试集负责「验」——验证必须用没参与过调参的数据，否则等于考自己出过的题（信息泄露）。
:::

## 参考文献

- 刑无刀、MatrixSpk：《XQuant：人人都是量化交易员》开源书稿，第 1 章「跑通第一个策略」（训练/测试切分、样本外验证、先猜后验）。[https://xingwudao.github.io/xquant-beginner/](https://xingwudao.github.io/xquant-beginner/)
- Datawhale：《WhaleQuant 量化开源课程》，第 7 章「量化回测」。[https://datawhalechina.github.io/whale-quant/](https://datawhalechina.github.io/whale-quant/)
- Backtrader：Python 开源回测框架，策略/指标/交易/数据模块的参考实现。[https://www.backtrader.com/](https://www.backtrader.com/) 及中文教程笔记 [learn_backtrader](https://github.com/jrothschild33/learn_backtrader)
