---
id: p2-l10
title: 阶段产出：独立回测
summary: "把第二章学到的全部组装起来——数据、策略、回测、绩效，交付一份完整报告。"
---

## 产出目标

本课交付一份**完整的策略回测报告**：选择一个标的、实现一类策略、跑通回测、呈现四类绩效指标。格式可复用第一章 p1-l11 的作业形态，但内容从「个股分析」升级为「策略分析」。

报告必须包含以下 5 个部分：

:::quiz
Q: 本课的阶段产出是?
- 一篇个股分析报告
- 一份完整策略回测报告：选标的、实现策略、跑通回测、呈现四类绩效指标
- 一段行情预测
- 一个回测框架源码库
A: 2
E: 产出从第一章的「个股分析」升级为「策略分析」，交付数据、策略、回测、绩效的完整闭环。
:::

## 报告结构

**1. 策略定义**：信号 → 持仓 → 交易三段式写清楚，参数值与其含义（如 MA20/60 中的 20、60 各代表什么），策略世界观（顺势 / 逆势 / 动量）。

**2. 数据与口径**：标的、回测区间、复权方式、成交时点（次日开盘价）、收益口径（复权连乘、不含首日、ddof=1、几何年化）——直接复用 p2-l5 的约定表。

**3. 交易记录**：列出每次信号切换的日期、方向、持仓变化与成交价。交易记录是回测报告里最容易被忽略却最能暴露问题的部分；一笔一笔核对，能发现前视、漏算成本等问题。

**4. 绩效指标**：p2-l6 的四类指标全表（收益、风险、风险调整、交易），与买入持有基准对比。

**5. 成本敏感性**：至少给出 0 / 5 / 10 / 20 bp 四档单边成本下的净收益（p2-l8 表格格式），并据此说明策略对成本的敏感度。

:::quiz
Q: 回测报告中「交易记录」部分的作用是?
- 只是形式要求，可有可无
- 逐笔核对日期、方向、持仓变化与成交价，能暴露前视、漏算成本等问题
- 仅用于展示成交量
- 替代绩效指标表
A: 2
E: 交易记录最容易被忽略却最能暴露问题，一笔一笔核对能发现前视、漏算成本等错误。
:::

## 自检清单

交付前逐项检查，任何一项不通过都要回去修：

- [ ] 数据来自 quantlab 真实库，代码可复算；
- [ ] 成交时点是信号次日开盘价（无前视）；
- [ ] 收益口径与全课程一致（不含首日、几何年化）；
- [ ] 报告同时列出毛收益与净收益（含成本假设）；
- [ ] 交易记录可逐笔核对，无凭空买卖；
- [ ] 参数敏感性已检查（最优参数周围 ±20% 依然成立）；
- [ ] 结论部分说明了策略的适用市况与失效条件。

:::quiz
Q: 下列哪项不属于交付前的自检清单内容?
- 数据来自 quantlab 真实库，代码可复算
- 成交时点为信号次日开盘价
- 报告同时列出毛收益与净收益
- 保证策略必然盈利
A: 4
E: 自检清单聚焦可复现、无前视、口径统一、成本透明、参数稳健与失效条件，而非承诺盈利。
:::

## 参考实现框架

```python
# 最小回测骨架（数据 → 信号 → 次日成交 → 净值）
import pandas as pd

df = pd.read_csv('moutai.csv', parse_dates=['trade_date'])
df['ret'] = df['pct_chg'] / 100                      # 复权收益
df['ma_fast'] = df['close'].rolling(20).mean()
df['ma_slow'] = df['close'].rolling(60).mean()
df['signal'] = (df['ma_fast'] > df['ma_slow']).astype(int)
df['pos'] = df['signal'].shift(1).fillna(0)          # 次日生效，杜绝前视
df['strat'] = df['ret'] * df['pos']                  # 策略日收益
df['nav'] = (1 + df['strat']).cumprod()              # 净值
```

输出报告的最终形式：净值曲线图 + 绩效指标表 + 成本敏感性表，标题注明数据口径。这份报告就是你的「量化能力的第一块里程碑」——从读懂指标，到能独立验证一个交易想法。

:::quiz
Q: 参考代码中 `df['pos'] = df['signal'].shift(1).fillna(0)` 的作用是?
- 让信号当日生效
- 让持仓信号次日生效，杜绝前视
- 计算均线
- 累乘净值
A: 2
E: `shift(1)` 把信号整体推迟一天，对应「次日成交」口径；`fillna(0)` 处理首日空仓。
:::

## 练习题

**练习 1（选择题）：** 报告结构中成本敏感性的要求。

:::quiz
Q: 阶段报告中「成本敏感性」部分的要求是?
- 至少给出 0 / 5 / 10 / 20 bp 四档单边成本下的净收益
- 只报最高一档成本下的收益
- 只报毛收益，不列成本假设
- 无需报告成本
A: 1
E: 成本敏感性至少给出四档单边成本下的净收益（p2-l8 表格格式），并据此说明策略对成本的敏感度。
:::

**练习 2（选择题）：** 自检清单中参数稳健的标准。

:::quiz
Q: 自检清单中「参数敏感性已检查」对应的标准是?
- 最优参数周围 ±20% 依然成立
- 参数取值越多越好
- 每个参数只测一次
- 参数必须是整数
A: 1
E: 好参数周围应是「脊」而非孤立「点」——最优参数附近 ±20% 依然成立才可信（p2-l7）。
:::

**练习 3（应用题）：** 补全回测代码并解释 shift 的作用。

:::exercise
T: 参考实现框架中 `df['signal']=(df['ma_fast']>df['ma_slow']).astype(int)` 得到当日信号。请补全 `pos`（次日生效）、`strat`、`nav` 三行代码，并解释 `shift(1)` 与 `fillna(0)` 各解决什么问题；若把 `pos` 写成 `df['signal']`（不做 shift），会引入哪类偏差？（追问）若某日标的因一字跌停而无法卖出（想清仓但实际没成交），而代码仍按 `pos=0` 计算当日收益，回测会高估还是低估净值？这种「信号与成交脱节」暴露的是什么问题？
:::

**练习 4（应用题）：** 审阅一份有缺陷的回测报告。

:::exercise
T: 某同学的回测报告写道：均线 MA20/60 回测茅台 2020-2026，「信号当日以收盘价成交」，绩效只报累计收益 +31.1% 与年化 4.4%，未提成本。请对照本课自检清单，指出该报告至少违反或遗漏了哪 3 项要求，并写出对应的改正建议。（追问）该报告还缺少「交易记录」部分。为什么逐笔交易记录比绩效指标表更能暴露前视与漏算成本的问题？请给出一条你会在交易记录里逐笔核对的具体检查项。
:::

**练习 5（选择题）：** 回测代码中 NaN 的传播。

:::quiz
Q: 参考回测代码中若删掉 `pos` 行的 `fillna(0)`，首日 $pos=\mathrm{NaN}$，后续 `strat=ret*pos` 与 `nav=(1+strat).cumprod()` 会怎样?
- 完全正常，不受影响
- 首日 strat 为 NaN，累乘从第 2 日起全部变成 NaN，整条净值曲线失效
- 只有首日异常，之后自动恢复
- NaN 只影响最后一天
A: 2
E: NaN 参与乘除与累乘会沿 cumprod 传播；`fillna(0)` 保证首日空仓收益为 0、净值从 1.0 起算，是代码可复算的关键细节。
:::