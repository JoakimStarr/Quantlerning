# Quantlerning 后端依赖
# Web 框架
fastapi>=0.115.0
uvicorn[standard]>=0.30.6
python-dotenv>=1.0.1
pydantic>=2.8.0
pydantic-settings>=2.0.0

# 数据库（只读连接 quantlab 库）
sqlalchemy>=2.0.36
asyncpg>=0.29.0

# 数据处理
pandas>=2.2.0
numpy>=1.26.0
scipy>=1.13.0

# 网络
httpx>=0.27.0

# 工具
cachetools>=5.3.0          # TTL 缓存（线程安全）
python-frontmatter>=1.1.0  # 课程 Markdown frontmatter 解析（依赖 PyYAML）

# 沙箱执行
# (仅白名单库，sandbox 内用受限环境)
