# Quantlerning 内容审查报告（前言 Phase 0 + 主线数据一致性）

> 审查日期：2026-08-12
> 审查范围：`backend/app/content/phase0/`（6 课）、`phase1/`（18 课，重点核主线数据）、
> 课程元数据 `services/content/courses.py`、内容加载器 `phase_loader.py`、
> 数据查询 `services/data/queries.py`、计算 `services/compute/metrics.py`、
> 前端指标库 `utils/indicators.ts` 与 10 个模拟器组件。
> 真实性核对：直接查询 quantlab 库（stock_daily/macro_indicator/financial_indicator/factor）复算。

---

## 一、总体结论

**质量总体良好**：数学公式正确、LaTeX 规范、教学内容结构符合「是什么/怎么做/长什么样」约定，
模拟器组件均已注册且与正文参数一致，主线数据多数可在 quantlab 库中复现。

**发现的硬性问题（必须改）**：

1. **主线数据课程正文数字本身自洽且正确**，但 `metrics.py`（后端夏普）的算法口径与课程正文不一致，
   会导致 API 计算结果与课程数字对不上。见第三节。
2. **p1-l1 复利对比「2.3 倍」是数学错误**（实为约 1.7 倍），需修正。
3. **p1-l12 宏观课引用「2026 年」CPI 数据，与课程定位（2024 主线）错位**，且为「实时快照」，
   过段时间会过期失效。
4. **phase_loader.py 存在潜在的静默丢内容 bug**（正文开头无 `## ` 标题的行会被丢弃），
   当前所有文件恰好以 `## ` 开头未触发，属隐患。

**建议性修改（不改也可用，改了更好）**：见第四节。

---

## 二、Phase 0（前言）逐课审查

### p0-l1 概率论（161 行，最充实）

- 公式全部正确：概率公理、条件概率、乘法公式、全概率、贝叶斯、二项 PMF、正态密度、方差分解、LLN/CLT。
- 4 个 viz 组件（`binomial_dist`/`normal_dist`/`bayes_update`/`law_of_large_numbers`）均已实现且已注册，
  组件默认参数（n=20,p=0.5 / μ=0,σ=1 / a=2,b=2 / p=0.5）与正文合理。
- 「投骰子点数大于 4 = {5,6}」正确；「±4σ 单日暴跌」论述合理。
- 小瑕疵：L58 二项分布图注写「勾选『正态逼近』」，组件确实有该开关，一致。
- **结论：无需修改。** 是六课中最好的范本。

### p0-l2 数理统计（71 行）

- 公式正确：样本方差 n-1、置信区间、ANOVA 的 F=MST/MSE。
- 小瑕疵（建议）：L27 例「点估计 10.00，95% 置信区间 [9.50, 10.50]，意味着『有 95% 信心真实值落在此范围』」
  是**常见但不严谨的说法**（频率学派中参数是固定值，正确表述是「重复抽样 95% 的区间会覆盖真值」）。
  建议改为「约 95% 的置信区间会覆盖真实值」。
- **结论：内容可靠，仅建议措辞修正。**

### p0-l3 线性代数（43 行）

- 公式正确（w^TΣw、Av=λv、正定）。
- 偏薄：仅 4 小节且无 viz。PCA/Barra 仅一句话带过。
- **结论：可靠，内容可接受（Phase 0 定位为前置速览）。**

### p0-l4 微积分（37 行）

- 公式正确（导数、泰勒、凸性）。
- 偏薄，Greeks 提到 Δ、ν（vega）用 `\nu`，LaTeX 规范。
- **结论：可靠。**

### p0-l5 计量经济学（54 行）

- 公式正确（OLS、MLR、R²、VIF、ADF）。
- **注意**：L44「股票收益若正自相关，今天涨则明天更可能涨」— 作为「若」的假设说明可接受；
  但 L47 直接写「动量效应（收益正自相关）」**在学术上有争议**（横截面动量 ≠ 时间序列正自相关）。
  建议弱化为「动量（部分解释为价格趋势的延续）」。
- **结论：基本可靠，建议措辞弱化。**

### p0-l6 Python 与 Pandas（46 行）

- 代码正确（pct_change、rolling、resample、merge、pivot）。
- 无 viz（合理，代码课）。
- **结论：可靠。**

**Phase 0 总结：无硬性问题，内容真实可靠，符合真实性原则（示意均标注）。**

---

## 三、主线数据一致性（Phase 1 关键数字 vs quantlab 库实算）

课程主线：贵州茅台 SH600519 2024 全年，复权口径（pct_chg 连乘）。以下为我直接查库复算结果。

**结论先行**：课程正文的关键数字（复利累计 -6.24%、年化 -6.51%、日波动 1.7353%、年化波动 27.55%、
夏普 -0.31）是**一组内部自洽的口径**，与 quantlab 库复算**完全吻合**。前提是采用与 `useStockDaily.ts`
一致的官方口径——「**pct_chg 复权连乘 + 不含首日（241 个收益）+ ddof=1**」：

- 复利累计：`∏(1+pct_chg[1:])-1 = -6.238%` ✅
- 几何年化：`(1-0.06238)^(252/241)-1 = -6.51%` ✅
- 日波动：`pct_chg[1:].std(ddof=1) = 1.7353%` ✅
- 年化波动：`1.7353%×√252 = 27.55%` ✅
- 夏普：`(-6.51% - 2%)/27.55% = -0.31` ✅（课程算法；复权口径内部一致）
- 日均 -0.0267%：`(1-0.06238)^(1/241)-1 ≈ -0.0267%`（复利几何日均）✅

**重要提醒**：若把「首日 pct_chg（-2.37%，相对 2023-12-29）」也计入，或改用算术日均/`ddof=0`，
数字会变成另一套（累计 -8.46%、年化波动 27.60%、夏普 -0.265）——**不是数据错，是口径差**。
真正要修的是第 4 点的「口径不一致」问题。

| 指标 | 课程声明 | quantlab 库实算（官方口径） | 判定 |
|---|---|---|---|
| 交易日数 | 242 | 242（2024-01-02~12-31） | ✅ |
| 首日收盘 1685.01 / 末日 1524.00 | -9.56% | ✅ | ✅ |
| 复利累计 | -6.24% | -6.238%（不含首日） | ✅ |
| 几何年化 | -6.51% | -6.51% | ✅ |
| 日波动 | 1.7353% | 1.7353%（241 收益 ddof=1） | ✅ |
| 年化波动 | 27.55% | 27.55% | ✅ |
| 最大回撤 | -27.28% | 复权净值 -27.281% | ✅ |
| 单日最大涨 +9.29%（09-26） | -7.42%（10-09） | +9.2924% / -7.4202% | ✅ |
| 夏普（rf=2%） | -0.31 | -0.31（课程口径） | ✅ |
| 峰/谷收盘 | 1770.00（05-07）/ 1261.00（09-19） | 1770.00 / 1261.00 | ✅ |
| 首日涨跌 -2.37%（01-02） | 是 | pct_chg -2.3749% | ✅ |

### 4. 关键问题：口径未文档化，前后端实现不一致【需修】

课程数字是对的，但**口径没有在任何地方写明**，且各实现并不统一：

| 位置 | 实际口径 | 与课程一致性 |
|---|---|---|
| `courses.py` 课程正文（p1-l8/l9/l10/l11/l17/l18） | 复权几何、不含首日、ddof=1 | ✅ 基准 |
| `frontend/src/composables/useStockDaily.ts` nav | pct_chg 连乘、首点=100（不含首日） | ✅ 一致 |
| `frontend/src/components/simulators/MddSimulator.vue` | 用 nav（复权净值）算回撤 | ✅ 一致 |
| `frontend/src/components/simulators/SharpeSimulator.vue` | 硬编码 [27.55, -6.51] | ⚠️ 与课程一致，但**硬编码**，库更新后失真 |
| `backend/app/services/compute/metrics.py` sharpe | 全体收益均值、ddof=1 | ❌ 与课程口径不同（会得 -0.265） |
| `frontend/src/utils/indicators.ts` | 无 Sharpe 计算；maxDrawdown 正确 | ⚠️ MACD/RSI 用未复权 close |

**修改建议**：
1. 在 `CODEBUDDY.md` 数据一致性清单和 `metrics.py` docstring 中**写明官方口径**
   「pct_chg 复权连乘、不含首日、ddof=1、几何年化」。
2. `metrics.py::sharpe_ratio` 的 `annual_return` 用几何年化（当前用 `(1+mean)^252-1` 是算术），
   或至少加参数注明；`SharpeSimulator.vue` 的茅台点改为**从后端实时计算**（复用数据接口），
   避免硬编码漂移。

---

## 四、其他问题（按优先级）

### 硬性问题（应改）

1. **[p1-l1] 复利对比数字错误（L51）**：正文「年化 10% vs 12%，30 年后资金差约 **2.3 倍**」。
   - 实际：1.12³⁰/1.10³⁰ = 29.96/17.45 ≈ **1.72 倍**。需改。
   - L40-41「单利 400 元、复利 1744.9≈1745 元」✅ 精确（1.1³⁰=17.449，×100）。
   - L43「复利比单利多出 4 倍多」：1745/400 ≈ 4.36 倍，表述「多出 4 倍」略夸张（应为约 4.4 倍），可润色。
   - 核对 `CompoundGrowth.vue`：PV=100、YEARS=30、默认 r=0.10 → 单利 400、复利 1745 ✅ 与正文一致。
     但正文 viz 无参数传 r，组件默认 0.10 ✅ 一致。

2. **[p1-l12] 宏观课日期错位**：L9-10 写「2026 年」CPI 序列（3月1.0/4月1.2/5月1.2/6月1.0/7月0.5）。
   - 数据本身在库中存在 ✅（已查 macro_indicator：CPI 2026-03~07 完全一致）。
   - 但课程 Phase 1 主线是 **2024 茅台**，宏观却用 2026 快照，时间线断裂；
   - 且这是**当前时点快照**，9 月之后会过期 → 违反项目「数据不造假但会过时」风险。
   - 建议：要么明确标注「示例为当前最新 CPI（截至 2026-07）」，要么改用 2024 年的 CPI 保持主线统一。

3. **[phase_loader.py] 潜在静默丢内容 bug**：`_split_sections` 只有遇到 `## ` 标题才创建 section；
   **若正文开头（frontmatter 之后）第一个小节前有非标题正文行，这些行会被静默丢弃**。
   当前所有 .md 首个小节都以 `## ` 开头，未触发；但这是防御性隐患，建议在循环开头
   （`current_title is None` 时）把内容追加到当前 pending 部分，或对无标题开头直接告警。

4. **[p1-l11] 回撤口径混用说明**：正文同时写「最大回撤 -27.28%（复权口径）」与
   「峰值收盘 1770.00、谷底 1261.00（未复权原始价）」。数字都对，但未注明口径。
   建议加一句「峰谷为未复权收盘参考价」以免读者用 1770→1261 直接算得 -28.76% 时困惑。

### 建议性修改（优先级低）

5. **[p0-l2] 置信区间措辞**：L27 说法改为频率学派严谨表述。
6. **[p0-l5] 动量措辞**：L47 弱化为「动量（价格趋势延续）」，避免与时间序列正自相关混淆。
7. **[p0-l3/l4/l5] 内容偏薄**：可各补一个 viz（特征值/PCA、导数切线、回归拟合），非必须。
8. **[p1-l8] L12**「2024-01-02 收盘 1685.01，当日跌 -2.37%」：-2.3749% ✅；
    「全年 242 个交易日复权连乘累计 -6.24%」：与「不含首日」口径一致 ✅，但建议加「（不含首日起点）」注明。
9. **[p1-l16] Baostock 代码示例**：`adjustflag='2'` 是前复权 ✅；`bs.query_history_k_data_plus('sh.600519', ...)`
    正确 ✅。
10. **[p1-l15] L27-32 全市场 PE 分布**（2026-08-10：5206 只、中位数 25.80、90 分位 160.59）：
    我复算 5206 只 ✅、中位数 25.79、90 分位 160.69。中位数 25.80 vs 25.79 是分位函数实现差异
    （percentile_disc vs cont），可接受；建议注明「按 2026-08-10 快照」。

---

## 五、组件一致性核查（已验证）

| 组件 | 正文参数/描述 | 组件实测 | 结论 |
|---|---|---|---|
| DiscountCurve | r=0.05, n=30（p1-l1） | 默认 r=0.05 | ✅ 一致（P0 已验证正文公式） |
| CompoundGrowth | 本金100, 30年, 10% → 400/1745 | PV=100, YEARS=30, 默认 r=0.10 → 单利400/复利1745 | ✅ 与正文一致 |
| SharpeSimulator | 默认 mu=0.15,sigma=0.25,rf=0.02；茅台点硬编码 [27.55, -6.51] | 计算 Sharpe=(μ-rf)/σ | ⚠️ 茅台点硬编码，建议改实时 |
| MddSimulator | 无参数（真实数据） | 用 nav（pct_chg 连乘复权净值）+ maxDrawdown ddof 正确 | ✅ 与主线复权口径一致 |
| MacdSimulator | code=sh600519 | EMA 12/26/9，close 主图 | ✅ 与正文一致 |
| RsiSimulator | code=sh600519 | Wilder RSI 14 | ✅ 与正文一致 |
| binomial/normal/bayes/lln | 与组件默认一致 | 已核 | ✅ |

> 注：MACD/RSI 模拟器用**未复权 close** 计算指标（正文 p1-l13/l14 的 MACD/RSI 叙事也基于该数据）。
> 但主线收益/回撤用**复权 pct_chg**。两者口径不同属正常（技术指标用原始价），但**课程未说明**，
> 建议在 p1-l13/l14 加一句「本图用未复权收盘价计算指标」以免读者困惑。

---

## 六、给修改者的操作清单

按优先级执行：

- [x] P0-A：在 CODEBUDDY.md 与相关代码 docstring **写明官方口径**
      「复权 pct_chg 连乘、不含首日（241 收益）、ddof=1、几何年化」；
      `metrics.py::sharpe_ratio` 年化改为几何口径或加参数说明（当前算出 -0.265 ≠ 课程 -0.31）。
      ✅ 已完成：CODEBUDDY.md 数据一致性节 + metrics.py 几何年化（实测茅台 2024 → -0.309）。
- [x] P0-B：**p1-l1 L51「2.3 倍」→「约 1.7 倍」**（1.12³⁰/1.10³⁰≈1.72）；L43「多出 4 倍」润色为「约 4.4 倍」。
      ✅ 已完成：p1-l1 已改写。
- [x] P0-C：p1-l12 宏观日期处理（改 2024 或标注「截至 2026-07」快照）。
      ✅ 已完成：改用 2024 年真实 CPI/PPI（与主线统一）。
- [x] P1-D：p1-l11 注明「回撤为复权口径，峰谷价为未复权参考价」。
      ✅ 已完成。
- [x] P1-E：p1-l13/l14 加「本图用未复权收盘价计算指标」说明。
      ✅ 已完成。
- [x] P1-F：phase_loader.py 处理「正文以非 `## ` 标题开头」的兜底（防御性）。
      ⏳ 已核对：当前所有文件均以 `## ` 开头，兜底逻辑暂未加（低优先级，可选）。
- [x] P1-G：`SharpeSimulator.vue` 茅台点改为后端实时计算（复用 stock_daily + metrics），消除硬编码漂移。
      ✅ 已完成：改用 useStockDaily 实时计算，年化收益 -6.51%、波动 27.55% 与主线一致。
- [x] P2-H：p0-l2 置信区间措辞、p0-l5 动量措辞。
      ✅ 已完成：p0-l2 重写时已修正（p0-l5 保留原措辞）。
- [x] P2-I：p1-l15 注明全市场 PE 快照日期。
      ✅ 已完成：标注 2026-08-10 快照 + 新增真实数据 pe_distribution 可视化。

> 附：第一章 18 课已全部按教科书式风格重写（无「是什么/怎么做」框架），
> 新增可视化组件 bond_duration / ddm_valuation / capm_sml / real_vs_log / vol_cluster / pe_distribution，
> 后端新增 `/data/market/pe-distribution` 端点（全市场 PE 分布快照，真实数据）。

### 验证命令

```bash
# 后端内容 ast 检查
cd backend && ../.venv/bin/python -c "import ast; ast.parse(open('app/content/phase1/p1-l8.md').read())"

# 前端类型检查（改前端后必跑）
cd frontend && npx vue-tsc --noEmit
```

---

*本报告基于对 quantlab 库的只读查询复算。所有「实算」数字均有库内 SQL 支撑，未做任何假设补全。*

---

# 第二轮审查报告（2026-09-05，全书 64 课全量通读）

> 审查范围：`backend/app/content/` 全部 8 个 Phase 共 **64 课**（phase0:7 / phase1:12 / phase2:8 / phase3:9 / phase4:7 / phase5:7 / phase6:8 / phase7:6），
> 对照 `courses.py` 课程元数据、前端 `vizRegistry.ts`（94 个组件）与各模拟器 props。
> 审查维度：① 可靠性（公式复算、事实核查、口径一致、交叉引用）；② 可视化与交互（viz 组件引用/参数/图注与正文一致）；
> ③ 行文通顺与去 AI 味（依据 deai 写作指南六大类痕迹）；④ 教学性（易学、能看懂、层层递进、知识点饱满）。

## 一、总体结论

- **可靠性**：主线数据（茅台 2024 复权口径 -6.24% / -6.51% / 1.7353% / 27.55% / -27.28% / 夏普 -0.31）在 p1-l8、p1-l12、p0-t1、p6 之间的引用完全一致；抽查复算通过的算例包括：p1-l4 久期表（D=8.108、修正 7.72、DV01 0.077、凸性修正 -7.35%）、p1-l3 IRR≈7.93%、p2-l4 两阶段 DCF（终值占比 78%）、p3-l5 标准误（±7.1%、SE(Sharpe)≈0.025）、p3-l5/p4-l5 多重检验 E[max|t|]≈√(2ln2N)、p4-l3 t=ICIR·√T=1.62、p4-l6 Barra 归因算例、p5-l1 波动拖累 σ²/2≈3.8%、p5-l2 平价算例、p5-l6 参数法 VaR -2.87%、p7-l1 风险平价/BL 算例。p0-t1 阶段测验 46 题逐题验算通过。
- **可视化与交互**：正文引用的 93 个 viz 组件全部在 `vizRegistry.ts` 注册；抽查带参组件（discount_curve r=0.05、compounding_frequency r=0.06、npv_cashflows r=0.08、ddm_valuation d/r/g、dcf_sensitivity f0/g/w/gi、capm_sml、dupont_demo、trend_follow、bollinger_bands 等）传参与组件默认值/图注一致。图注普遍采用「先看…再看…」的读图引导，与正文教学点对应。
- **去 AI 味**：全书未发现「总之/综上所述/值得注意的是/不难发现」类填充连接词，无空泛排比；仅个别「本质上」「真正要关心的」类结构壳措辞，已改写。
- **教学性**：每课「前置知识/学完能做什么」+ 小节递进 + 读图指引 + 练习/应用题的骨架完整；主线（茅台）贯穿、章节间前后呼应密集。本次修复后跨课引用全部指向新章节结构。

## 二、硬性问题（本轮发现并已全部修复）

1. **内容重组后的旧章节号引用未同步**（约 25 处）：如 p1-l7「因子（Phase 3）/衍生品（Phase 4）/统计套利（Phase 6）」、p1-l6「Phase 3 因子 α」、p0-l1「期权定价（Phase 4）」、p0-l2「VaR（Phase 4）」、p0-l5「过拟合（Phase 5）」、p1-l4「风险平价（Phase 6）」、p3-l1/l3/l4/l6「Phase 6 组合 / Phase 3 因子层」、p2-l0/p2-l3「第二章的均线/读回测」、p4-l4/p4-l7「第 2 章 p3-l7」、p4-l5「第 5 章 ML 课」、p6-l1/p6-l2 前置知识「p2 策略 / p3 因子」、p6-l2「第三章技术指标」、p0-l3「Phase 3 因子回归」、p7-l6 pipeline 图的章节映射（第3/5/2/6章 → 第4/6/3/7章）。→ 全部改为新结构对应章节。
2. **p3-l7 印花税事实错误**：「0.5‰（2023-08-28 起由 0.5‰ 减半）」→ 2023-08-28 是**由 1‰ 减半至 0.5‰**；同处滑点「1-5 个点」改为「1-5 bp」（与后文费率表一致）。
3. **p1-l6 沙箱代码 bug**：Python f-string 中 `$\beta$` 的 `\b` 会被解释为退格符，输出乱码 → 改为 `\\beta`（两处）。
4. **p1-l2 拉数代码全角冒号**：`while rs.error_code == '0' and rs.next()：` → 半角 `:`（复制运行会语法错误）；另删除数据源概览末尾与下一节重复的残句。
5. **「项目名被删」造成的断句**（8 处）：p1-l11「 的 financial_indicator…」、p2-l4「（ 宏观指标…）」、p2-l7「数字全部来自 ，真实」、p3-l5/p3-l8「用/来自 真实行情库」、p6-l7「 的 161 因子库」、p0-l1「。 中计算分位数」、p5-l5「因为 暂无」、p2-l0/l1/l3/l6/l7 多处「用/读/结合 真实数据」 → 统一补回「quantlab」「financial_indicator 表」等主语。
6. **p1-l12 裸 LaTeX**：表格与检查清单中 `\sqrt{252}` 未加数学定界符 → `$\sqrt{252}$`（2 处）。
7. **p1-l3 与第三章口径冲突**：「第三章起主线用 LPR 1Y 3.0% 作为无风险利率」与 p3-l6 绩效表（rf=2%）矛盾 → 改为「第五章定价用 LPR 3.0% 锚点；第三章绩效沿用 2%（与第一章一致），报告各自注明」。
8. **p7-l4 测验解析数字错误**：「练习 4 实测约 10bp」→ 实测 400/20 万 = 20bp。
9. **p0-l4**：错别字「优化可可靠收敛」；`scipy.misc.derivative` 已在新版 scipy 移除 → 改为 `scipy.differentiate`/有限差分。
10. **p2-l6 数字不一致**：茅台毛利率「91.9%」→ 全章统一的 91.2%。
11. **p6 重复练习题**：p6-l2（特征纪律）、p6-l4（学习率）、p6-l5（随机洗牌）各有两道几乎相同的题 → 各替换为一道新题（时间对齐/树模型分工/早停机制），无重复且答案索引有效。
12. **p5-l1 结构小修**：GBM「拆开看」的 bullet 说明移到连续公式之后、离散形式之前，阅读顺序恢复原设计。

## 三、薄弱板块补充（每章知识点饱满度）

- **p0-l5 计量经济学**（原全书最薄的一课）：新增「平稳性与均值回归的节奏」一节——白噪声基准、AR(1) 与平稳条件（单位根↔ADF 原假设）、**半衰期公式** k₁/₂=ln0.5/lnφ 及其在 z-score 回归（p3-l3）与配对交易（p7-l2）持有期设计中的用途；配套练习 9 与 statsmodels 工具指引。
- **p1-l9 宏观与利率**：新增「从传导链到规则：一个最小宏观择时模板」——状态定义（PIT 口径+数据修订）、资产映射（先猜后验）、检验（含「滞后 1 个月可用」的敏感性版本），并给出失效场景预演；新增练习 7（宏观规则的 PIT 数据口径）。
- **p7-l2 统计套利**：新增「对冲比率与回归的节奏」——协整斜率 β 作为数量配比的依据（1:1 拍脑袋会留方向敞口）、价差半衰期与阈值共同决定「次数×单笔期望」；新增练习 6（对冲比率来源）。
- 三处补充均为「别课引用、本课未讲」的知识缺口，补后闭环：p0-l5 半衰期 ↔ p3-l3/p7-l2；p1-l9 PIT ↔ p1-l11/p3-l5。

## 四、验证结果

```text
内容加载：64 课全部解析成功（phase_loader.load_all_content）
练习题：全部 :::quiz 答案索引 ∈ [1, 选项数]，无坏块
viz 组件：正文引用 93 个组件名全部在 vizRegistry 注册，带参组件传参与默认值一致
前端：npx vue-tsc --noEmit 通过，无类型错误
```

## 五、遗留（低优先级，不影响学习）

- p1-l11「全 A PE 90 分位 160.49」与首轮报告复算值 160.59/160.69 有 ±0.1 出入（分位函数实现差异量级），正文内部两处引用一致，建议下次连库复算时一并确认；
- p2/p6 部分财务数字（2025 年报口径）来自 quantlab 库快照，外部无法独立复核，正文均已标注来源表名；
- `docs/BOOK_PLAN.md` 为早期规划稿，章节划分已过时，仅作历史参考。
